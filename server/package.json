{
  "name": "apextuts-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "seed": "node db/seed.js",
    "test": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "better-sqlite3": "^12.9.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "supertest": "^6.3.4",
    "vitest": "^1.2.2"
  }
}
